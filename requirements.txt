# Botkin Dependencies

# Core (Telegram Bot)
aiogram>=3.0.0
python-dotenv>=1.2.2  # <1.2.2: GHSA-mf9w-mj56-hr94 (symlink-following в set_key)
pyyaml>=6.0
requests>=2.33.0  # <2.33: GHSA-9hjg-9r4m-mvj7 (.netrc leak), GHSA-gc5v-m9x4-r6x2 (temp-file reuse)

# AI Services
anthropic>=0.109.0
openai>=1.0.0

# Data Processing (Scripts)
garminconnect==0.2.40
garth>=0.4.47
lnetatmo>=4.2.0
pandas>=2.0.0

# Development & Testing
pytest>=7.0.0
pytest-asyncio>=0.23.0
mypy>=1.0.0
ruff>=0.15.0
pre-commit>=4.0.0
types-requests
types-PyYAML>=6.0

# Database (Multi-user Support)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.12.0

# API (Apple Health Integration)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# JWT auth (BotkinClaw — ex-NanoClaw, см. ADR-0002)
# >=2.13.0: GHSA-752w-5fwx-jx9f (unknown crit header) + серия PYSEC-2026 (auth)
pyjwt>=2.13.0

# Rendering PNG-отчётов агентом (см. core/reports/biomarker_dynamics.py)
matplotlib>=3.8
defusedxml==0.7.1

# PDF → изображения (анализы крови в PDF от лабораторий, конвертация для vision-пайплайна)
pymupdf>=1.24.0

# CGM-глюкоза: клиент LibreLinkUp API (Abbott FreeStyle Libre 3), см. #96.
# Пин minor: unofficial API, между минорами менялись auth и URL — апгрейд только с ревью диффа
# (клиент шлёт креды dr@botkin.health на эндпоинт Abbott; смена URL = утечка кредов).
pylibrelinkup>=0.10.0,<0.11
