"""Single source of truth for Botkin version.

Bump via `python scripts/bump_version.py X.Y.Z` — он также правит landing.
SemVer: MAJOR.MINOR.PATCH. До v1.0.0 — pre-stable (API/схема может меняться).
"""

__version__ = "0.5.0"
