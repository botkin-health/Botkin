# AI_CHANGELOG — технические изменения BotkinClaw

Этот файл ведёт историю технических изменений агента (промпты, tools, архитектура).
Для пользовательских изменений — Notion Хронолог (ID `37bf1efb-961b-81cd-9145-cc24bca86e96`).

---

## 2026-06-20 — #183 #184 #185 / PR #186 #187

**Prompt guards: fallback поиска еды, объяснение дашборда, meal_time по фразе**

- `core/agent_chat.py`: добавлен блок `# 📅 ПОИСК ЕДЫ: НЕ СДАВАЙСЯ ПОСЛЕ ПЕРВОГО ЗАПРОСА`
  — если `get_recent_meals(days=1)` вернул пустой список, агент обязан повторить
  с `days=3` перед выводом «данных нет». Прецедент: Кристина @ochkina пишет утром
  про вчерашнюю еду, агент говорил «ничего нет».
- `core/agent_chat.py`: добавлен блок `# 📊 ДАШБОРД — ЭТО СТАТИСТИКА, НЕ ЖИВОЙ ЛОГ`
  — объяснение что дашборд показывает 7–30-дневные тренды, а не сегодняшний лог;
  запрет отвечать «обновляется с задержкой».
- `telegram-bot/handlers/text.py`: функция `_temporal_phrase_to_time()` — при записи
  еды через текстовый роутер `meal_time` инферится из фразы («вечером» → 20:00,
  «утром» → 08:00, «в обед» → 13:00, «ночью» → 22:30); fallback — текущее время.

---

## 2026-06-19 — #181 / PR #182

**Гард галлюцинаций состава еды и ложного ✅**

- `core/agent_chat.py`: добавлен блок `# 🍽️ ЕДА: ОТОБРАЖЕНИЕ И РЕДАКТИРОВАНИЕ`
  в `UNIVERSAL_META_PROMPT`. Запрет: (1) генерировать ингредиенты не из
  `items` ответа `get_recent_meals`; (2) говорить «✅ Готово» без вызова
  `edit_meal`; (3) переспрашивать уже подтверждённый выбор в диалоге.
- `telegram-bot/webhook/agent_tools_api.py`: compact mode `recent_meals` теперь
  читает ключ `food` с приоритетом перед `product`/`name` — composite items
  (фото-логи) больше не возвращают пустое имя.
- `tests/test_ask_agent.py`: 3 новых теста покрывают оба изменения.

Прецедент: Кристина @ochkina, 19.06.2026 — агент показал рис+куриная грудка
вместо реального «Боул с киноа, креветками и авокадо» и подтвердил
«перенесено в обед» без UPDATE в БД.
