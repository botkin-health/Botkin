# HealthVault Project: Status & Roadmap

> **💡 Context**: Этот файл является единым источником правды о состоянии проекта. 
> Начинай любую работу с чтения этого файла.

---

## 🔥 Приоритетные Задачи (Следующие Шаги)

1. **Завершение миграции Postgres**: 
   - [ ] Перевести Вес (`weights.py`) и Еду (`nutrition.py`) с JSON на БД. 
   - *Status*: Витамины и Отчеты уже мигрированы. Схема v2 готова.

2. **Аналитика**: 
   - [ ] Команда `/analytics` с корреляциями (Давление vs Сон/Вес).
   - [ ] Интегрировать скрипт корреляции.

3. **Качество кода**: 
   - [ ] Внедрить Pydantic для строгой типизации (снижение ошибок "вайбкодинга").
   - [ ] Тесты на критические модули (OCR, подсчет итогов).

### 🐛 Известные Баги
- [ ] **Вес Бутылки / Стандартной Тары**: LLM иногда ошибается с весом (100г вместо 750г).
  - *Workaround*: Указывать объем явно (напр. "750 мл шампанского").
  - *Fix*: Реализовать жесткий override веса через Regex.

---

## 📜 История Проекта (Ключевые Вехи)

**Январь 2026**
- ✅ **Создание MVP**: Локальный запуск бота, базовый учет калорий.
- ✅ **Первый Сервер**: Деплой на российский хостинг, настройка Docker.

**Февраль 2026**
- ✅ **Переезд в Нидерланды**: Миграция на VDSina (NL) для обхода блокировок OpenAI/Gemini.
- ✅ **Голосовой Ввод**: Подключение транскрибации (Whisper/Gemini) для логирования голосом.
- ✅ **Интеграция Garmin**: Синхронизация шагов, сна и активных калорий.
- ✅ **Масштабирование**: Расширение диска сервера (10GB → 40GB) для стабильности.
- ✅ **База Данных**: Запуск PostgreSQL, частичная миграция (Витамины, Еженедельные отчеты).
- ✅ **Исправление Логики**: Фикс подсчета итоговых калорий и веса продуктов.
- ✅ **Рефакторинг (этап 1)**: Удалён мёртвый код — неиспользуемые импорты storage в handlers, модули `common_config.py`, `biometrics.py`.
- ✅ **Рефакторинг (этап 2)**: Удалён мёртвый `handlers/health.py`; в Docker добавлен `infrastructure/` (кэш изображений в проде); `core/storage.py` помечен как legacy.
- ✅ **Рефакторинг (этап 3)**: Удалена папка `telegram-bot/scripts/debug/` (сломанные импорты); единый источник для env — корневой `.env.example`, в telegram-bot — ссылка.

---

## 🧹 Обслуживание и Инфраструктура
- [x] **Очистка старого сервера (РФ)**: Удалены данные с 146.103.111.109.
- [x] **Устранение дублей**: Idempotency middleware внедрен.
- [x] **Мониторинг диска**: Место увеличено до 40GB.
- [ ] **Backup**: Настроить автоматическое резервное копирование БД PostgreSQL.

---

## 🔧 Рефакторинг бота (план)

> **Цель**: Сократить лишние файлы, уменьшить объём кода, убрать неиспользуемое.  
> **Не запускать** изменения до отмашки — только план и чек-листы.

### 1. Мёртвый код и неиспользуемые импорты ✅ (2026-02-18)

- [x] **Удалить неиспользуемый импорт `core.storage` в handlers**
  - `handlers/commands.py`: импорт `get_today_totals` не используется (/day берёт данные из `nutrition_service`).
  - `handlers/photo.py`: импорты `add_meal`, `get_today_totals` не используются (сохранение через `save_meal_to_db`).
  - Действие: убрать эти импорты.

- [x] **Удалить неиспользуемые модули core**
  - `core/common_config.py` — нигде не импортируется (дублирует `config.get_settings()`).
  - `core/biometrics.py` — класс `BodyMetrics` нигде не используется.
  - Действие: удалить оба файла (или перенести biometrics в archive, если планируется использование).

### 2. Legacy storage и JSON-логика ✅ (2026-02-18)

- [x] **Свести к минимуму или убрать `core/storage.py`**
  - Импорты из handlers убраны в этапе 1. В активном коде storage не вызывается. Добавлен docstring: legacy, прод на PostgreSQL.
  - Действие: оставлен для совместимости (archive-скрипты); при желании позже удалить.

- [ ] **Уточнить про `core/weights.py`**
  - В ROADMAP указано «Удаление устаревшей JSON логики из core/weights.py» — файла `core/weights.py` в репозитории нет. Закрыть пункт в ROADMAP или искать остатки в других файлах.

### 3. Обработчики и роутеры ✅ (2026-02-18)

- [x] **handlers/health.py**
  - В `bot.py` роутер health не подключался. Файл удалён (функциональность Apple Health через бота при необходимости можно вернуть из git history).

### 4. Domain и инфраструктура (JSON-репозиторий)

- [ ] **domain/ и infrastructure/storage/json_repository.py**
  - Используются только в тестах (`test_repository.py`, `test_domain_models.py`). Продакшн использует `database/repository.py` и `helpers/db_save.py`.
  - Действие: оставить тесты как проверку legacy-пути ИЛИ переписать тесты на DB-репозиторий и удалить `domain/`, `infrastructure/storage/json_repository.py` (и при необходимости интерфейс в `domain/interfaces.py`).

- [x] **Dockerfile и infrastructure** (2026-02-18)
  - В образ добавлен `COPY infrastructure/ ./infrastructure/` — кэш изображений (image_cache) теперь доступен на сервере.

### 5. Скрипты и отладочные файлы ✅ (2026-02-18)

- [x] **telegram-bot/scripts/debug/**
  - Скрипты с устаревшими путями удалены (reproduce_*.py, verify_nutrition_v2.py, manual_transcribe.py и др.). При необходимости можно восстановить из git history.

- [ ] **Корневые scripts/**
  - Много одноразовых миграций и анализов. Оставлено без изменений; при желании позже перенести часть в archive.

### 6. Archive

- [ ] **archive/**
  - Не трогаем без необходимости. В Docker/деплой не входит.

### 7. Дублирование и конфиг ✅ (2026-02-18)

- [x] **Два .env.example**
  - Источник правды — корневой `.env.example`. В `telegram-bot/.env.example` оставлена краткая ссылка на корень.

- [ ] **Дублирование логики ключей/настроек**
  - Уже унифицировано через `config/settings.py`. Проверить, что нигде не осталось захардкоженных путей к ключам кроме config.

### 8. Крупные модули (по ROADMAP)

- [ ] **Рефакторинг core/nutrition.py**
  - В ROADMAP: «слишком много ответственностей». Уже вынесены `llm_food_processor`, `menu_meal_processor`. В nutrition остаются парсинг, расчёт КБЖУ, реэкспорты.
  - Действие: разбить на подмодули (например, nutrition/calculation.py, nutrition/parsing.py) или явно описать зоны ответственности в docstring и не трогать, пока не появятся новые фичи.

### 9. Тесты

- [ ] **Тесты с устаревшими путями**
  - `run_nutrition_snapshot_tests.py`, `verify_fix_nutrition.py` используют `services.nutrition` и старые имена.
  - Действие: обновить импорты на `core.nutrition` (и актуальные имена) или пометить тесты как legacy и вынести в archive.

- [ ] **test_repository.py и test_domain_models.py**
  - Завязаны на JSON-репозиторий и domain. При удалении domain/json_repository — переписать или удалить.

### 10. Чек-лист перед запуском рефакторинга

- [ ] Сделать полный прогон тестов: `pytest` (или текущая команда).
- [ ] Убедиться, что деплой и перезапуск бота на сервере выполняются после изменений (`./deploy.sh`).
- [ ] Рефакторить по шагам (не всё сразу): сначала импорты и мёртвые файлы, потом storage/domain, потом скрипты и archive.

---

## 👤 Личное Здоровье (Цели 2026)

### 🎯 Тело и Форма (Рост 170 см, Возраст 48 лет)
- **Вес**: 
  - 📉 **Цель: 70-72 кг** (BMI ~24.5, "комфортный вес" для 48 лет).
  - *Расчет*: Идеальный по формулам ~66 кг, но корректируем на +4-6 кг за счет мышц.
- **Композиция**:
  - Жир: Снизить **< 20%** (текущий ~28%).
  - 🏋️‍♂️ **Мышцы**: Прирост мышечной массы (повышение BMR, поддержка суставов).
- **Висцеральный жир**: 
  - 📉 **Цель: < 10** (Критично: в 48 лет висцеральный жир >10 резко повышает риск инфарктов). 
  - *Стратегия*: Только дефицит калорий + кардио (этот жир уходит первым).

### 🩸 Биомаркеры и Риски
- **Холестерин**: 📉 Снизить **LDL < 3.0** (текущий ~3.79), **Общий < 5.0**.
- **Давление**: 📉 Стабилизировать **< 125/80**.
- **Инсулинорезистентность**: Снижение висцерального жира улучшит чувствительность к инсулину.
- **Анализы**: Регулярный чек-ап для контроля динамики (липидограмма).

### 🔋 Образ Жизни и Энергия
- **Сон**: 
  - 🛌 **7+ часов** регулярно. Высокое качество (Deep/REM).
  - 🌙 **Режим**: Отбой до 00:00.
  - 🔇 **Храп**: Снизить/Убрать (прямая связь с весом и алкоголем).
- **Garmin Body Battery**: 🔋 Улучшить средний показатель "заряда" (меньше стресса, лучше отдых).
- **Питание**:
  - **Чистое питание**: Без срывов переедания.
  - **Белок**: > 150г (строительный материал для мышц).
  - **Алкоголь**: 📉 Минимизировать. Если есть — мало и без закусок (переедания).
- **Восстановление**: 🧖‍♂️ Баня/Массаж (1 раз в неделю).

